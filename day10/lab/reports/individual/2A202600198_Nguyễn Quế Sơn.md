# Báo Cáo Cá Nhân — Lab Day 10: Data Pipeline

**Học sinh:** Nguyễn Quế Sơn (2A202600198)  
**Nhóm:** C401 - D6  
**Vai trò:** Documentation / Observability Owner
  
**Run ID:** `final-fix`

---

## 1. Phần phụ trách cụ thể

Trong phiên làm việc Day 10, tôi chịu trách nhiệm chính về việc thiết lập và quản lý hệ thống tài liệu (Data Documentation) và báo cáo chất lượng (Quality Reporting). Cụ thể:
- **Tài liệu Hợp đồng dữ liệu (`docs/data_contract.md`):** Xây dựng Source Map cho 2 nguồn dữ liệu chính (CSV và Local files), định nghĩa Schema cho tầng Cleaned và quy hoạch ranh giới xử lý (Ingest/Transform/Quality).
- **Báo cáo chất lượng (`docs/quality_report.md`):** Theo dõi và đối chứng số liệu giữa các lần chạy (Standard run vs Inject corruption run), tổng hợp bằng chứng before/after cho phần evaluation.
- **Báo cáo nhóm (`reports/group_report.md`):** Điều phối thông tin từ các thành viên khác (cleaning rules, expectations, eval results) để hoàn thiện báo cáo tổng kết của nhóm C401-D6.

## 2. Một quyết định kỹ thuật tiêu biểu

**Quyết định:** Thiết kế cấu trúc Data Contract phân tách rõ ranh giới giữa Ingestion và Transformation.
- **Bối cảnh:** Nhóm gặp khó khăn trong việc xác định xem lỗi "Missing exported_at" nên được xử lý ở bước kéo dữ liệu hay bước làm sạch.
- **Lý do:** Tôi quyết định đưa kiểm tra này vào Data Contract như một tiêu chuẩn bắt buộc của tầng Cleaned Schema. Việc này giúp tách biệt trách nhiệm: người làm Ingest chỉ cần kéo data, nhưng người làm Transform có quyền từ chối (Quarantine) nếu metadata quan trọng này bị thiếu.
- **Kết quả:** Pipeline trở nên minh bạch hơn, việc truy vết lỗi Fresness trở nên dễ dàng nhờ metadata `exported_at` luôn được đảm bảo có sẵn.

## 3. Một sự cố / Anomaly phát hiện

**Sự cố:** XML/BOM encoding trong file export làm sai lệch kết quả hash `chunk_id`.
- **Phát hiện:** Khi Tấn (Eval Owner) chạy thử pipeline, cùng một nội dung nhưng `chunk_id` thay đổi liên tục làm hệ thống embed không idempotent.
- **Xử lý:** Tôi đã phối hợp cùng Khánh (Retrieval Owner) để đưa thêm Rule 9 vào `cleaning_rules.py` nhằm loại bỏ ký tự BOM trước khi hashing. Đồng thời, tôi cập nhật Data Contract để quy định UTF-8 là encoding bắt buộc duy nhất.
- **Bằng chứng:** Sau khi fix, log `embed_prune_removed` trả về 0 trong các lần rerun với cùng dữ liệu, chứng minh tính ổn định của hashing.

## 4. Bằng chứng Before / After

Trích xuất từ kết quả kiểm thử của nhóm:
- **Before (Inject):** Query `q_refund_window` trả về `hits_forbidden=yes` (14 ngày).
- **After (Clean):** Quality Gate (E3) và Rule 6 làm việc, trả về `hits_forbidden=no` (7 ngày).
- **Minh chứng vật lý:** Trực tiếp đối soát file `anomaly-test_result.jsonl` và `final-fix.json` để khẳng định sự thay đổi của `hits_forbidden` từ `true` sang `false`.

## 5. Cải tiến 2h (nếu có thêm thời gian)

Nếu có thêm 2 giờ, tôi sẽ:
1. Tự động hóa việc trích xuất bảng `metric_impact` trực tiếp từ file Log vào `quality_report.md` bằng một script Python nhỏ thay vì phải điền tay.
2. Xây dựng một Dashboard HTML đơn giản trực quan hóa các 5 pillars of observability (Freshness, Volume, Distribution, Schema, Lineage) cho nhóm.
